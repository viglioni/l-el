#+title: l.el core

In this directory we define the main macros to create =ldef=, which has pattern matching, type matching and autocurry and also =l-syntax= that allows function calls without calling =funcall= and =ldef= documentation elixir style.

For complete documentation and examples, see the [[../../docs/api.org][API Reference]].
